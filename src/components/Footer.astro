---
import { Icon } from "astro-icon/components";

const currentYear = new Date().getFullYear();
---

<footer class="py-8 mt-auto">
  <div class="container mx-auto px-4">
    <div class="text-center mb-3">
      <a href="/work-links" class="inline-flex items-center gap-2 px-3 py-2 text-sm" style="color: var(--color-text-muted);">
        <Icon name="fa6-solid:briefcase" class="w-3 h-3" />
        Work Links
      </a>
    </div>
    <div class="flex items-center justify-center gap-2">
      <p class="text-sm pt-1" style="color: var(--color-text-muted);">
        © {currentYear} Apirak Panatkool. All rights reserved.
      </p>

      <!-- RSS Feed Link -->
      <a
        href="/rss.xml"
        class="px-3 py-2 transition-colors hover:opacity-70"
        aria-label="RSS Feed"
        title="Subscribe to RSS Feed"
        style="color: var(--color-text-muted);"
      >
        <Icon name="fa6-solid:rss" class="w-4 h-4" />
      </a>

      <!-- Theme Toggle Button -->
      <button
        id="theme-toggle"
        class="px-3 py-2 transition-colors hover:opacity-70"
        aria-label="Toggle theme"
        title="Toggle theme"
        data-theme="auto"
        style="color: var(--color-text-muted);"
      >
        <Icon
          name="fa6-solid:sun"
          class="w-4 h-4 theme-icon"
          data-theme-icon="light"
        />
        <Icon
          name="fa6-solid:moon"
          class="w-4 h-4 theme-icon"
          data-theme-icon="dark"
        />
        <Icon
          name="fa6-solid:circle-half-stroke"
          class="w-4 h-4 theme-icon"
          data-theme-icon="auto"
        />
      </button>
    </div>
  </div>
</footer>

<style>
  /* Theme toggle icons */
  .theme-icon {
    display: none;
  }

  #theme-toggle[data-theme="light"] .theme-icon[data-theme-icon="light"],
  #theme-toggle[data-theme="dark"] .theme-icon[data-theme-icon="dark"],
  #theme-toggle[data-theme="auto"] .theme-icon[data-theme-icon="auto"] {
    display: inline-block;
  }
</style>
